export PATH=~/env/bin:~/.pipx/bin:/usr/bin
export LANG=en_US.UTF-8
export TERM=xterm-256color

export EDITOR=vi
export BROWSER=browser
export PAGER=bat

eval "$(xdg-dirs)"
eval "$(mise activate bash)"
eval "$(keychain --quiet --eval --agents ssh id_rsa)"
