export PATH=~/env/bin:/usr/bin:~/.asdf/bin
export LANG=en_US.UTF-8
export TERM=xterm-256color

export EDITOR=vi
export BROWSER=brave
export PAGER=bat

eval "$(xdg-dirs)"
eval "$(direnv hook bash)"
eval "$(keychain --quiet --eval --agents ssh id_rsa)"
