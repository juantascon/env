#! /bin/bash

unset LD_PRELOAD

echo "running override zoom"
url="zoommtg://zoom.us/join?action=join&confno="

case "$1" in
    [1-9]*)
            exec /usr/bin/zoom "${url}${1}" ;;
        *)
            exec /usr/bin/zoom "$@";;
esac
