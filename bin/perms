#! /bin/bash

usage()
{
	echo -e "Uso: $(basename $0) <dir>|<file> ..."
	exit $*
}

[ -z "$1" ] && usage

find "$@" -type f | while read f; do chmod 600 "$f"; done
find "$@" -type d | while read d; do chmod 700 "$d"; done
